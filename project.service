[Service]
Environment="APP_SECRET_KEY=change-me-in-prod"
Environment="APP_DEMO_USER=admin"
Environment="APP_DEMO_PASS=admin"

User=ec2-user
Group=ec2-user
WorkingDirectory=/home/ec2-user/class25-26-project2


ExecStart=/bin/bash -c 'source .venv/bin/activate && cd app && uvicorn main:app --host 127.0.0.1 --port 9000'

Restart =always
RestartSec=5